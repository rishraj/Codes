def my_func(arr, k):
    m = max(arr)
    m_ind = []
    for i in range(len(arr)):
        if arr[i] == m:
            m_ind.append(i)
    t = len(m_indices)
    while t>=0 and k>=0:


for _ in range(int(input())):
    k = int(input())
    arr = list(int(x) for x in input().strip())
    my_func(arr, k)